# Required for cloud-powered Live Mode. Keep these values out of source control.
DEEPGRAM_API_KEY=
# Optional when using a standalone LibreTranslate instance instead of Docker Compose.
LIBRETRANSLATE_URL=http://libretranslate:5000
